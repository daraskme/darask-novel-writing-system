{
  "name": "darask-novel-writing-system",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build:reader": "wrangler pages functions build --outdir .wrangler/reader-build"
  },
  "dependencies": {
    "diff": "9.0.0",
    "jose": "^6.1.0"
  },
  "devDependencies": {
    "miniflare": "5.20260918.0-alpha",
    "wrangler": "^4.0.0"
  }
}
